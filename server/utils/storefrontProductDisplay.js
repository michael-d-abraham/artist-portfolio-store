const {
    displayProductName,
    primaryProductImageUrl
} = require('../../shared/productDisplay');
const { formatMoneyFromCents } = require('../../shared/money');

module.exports = {
    primaryProductImageUrl,
    displayProductName,
    formatMoneyFromCents
};
